package main

import (
	"fmt"
	"math"
)

// imports brings external packages
// where actually u need it

func main() {

	// packageName. Functionname -> call a function from a package

	fmt.Println("square root of 16 is:", math.Sqrt(16))
}
